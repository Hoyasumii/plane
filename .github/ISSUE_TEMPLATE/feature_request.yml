name: Feature request
description: A missing endpoint, tool, command or option.
labels: ["enhancement"]
body:
  - type: dropdown
    id: area
    attributes:
      label: Area
      options:
        - SDK — v1
        - SDK — v2
        - MCP server
        - CLI
        - Docs site
    validations:
      required: true
  - type: textarea
    id: problem
    attributes:
      label: What you are trying to do
      description: The task, and what gets in the way today.
    validations:
      required: true
  - type: textarea
    id: proposal
    attributes:
      label: What you would like
      description: The call, tool or command you have in mind. For a Plane endpoint the SDK does not cover, link its page in the Plane API docs.
  - type: checkboxes
    id: contribute
    attributes:
      label: Contributing
      options:
        - label: I would like to open a pull request for this.
